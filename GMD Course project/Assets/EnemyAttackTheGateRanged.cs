using System;
using StarterAssets.Interfaces;
using UnityEngine;
using UnityEngine.AI;

public class EnemyAttackTheGateRanged : MonoBehaviour, IEnemyAttackTheGate
{
    // Start is called before the first frame update
    private void Start()
    {
    }

    // Update is called once per frame
    private void Update()
    {
    }

    public void AttackGate(NavMeshAgent agent)
    {
        throw new NotImplementedException();
    }
}